colors = ["red", "green", "blue"]

for color in colors:
    print(color)

for index, color in enumerate(colors):
    print(f"Color {index+1}: {color}")
    # Normally index starts at 0. +1 makes it start at 1.